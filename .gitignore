# Node modules
node_modules/

# Build output
dist/*
package/
# 例外：Parvis 写作模式消费的自包含 bundle 产物需随 git 分发，
# 以便通过 github: 依赖安装后开箱即用（dist 其余产物仍忽略）。
# 注意此处用 dist/* 而非 dist/，否则 git 不递归进被忽略的目录，下面的例外不生效。
!dist/parvis-editor.js
!dist/parvis-editor.css

# Editor directories and files
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Cache
.npm
.eslintcache
.stylelintcache
.prettiercache
vite.config.js.timestamp*

# Husky
.husky/_

# Auto Imports
types/imports.d.ts
types/components.d.ts

# Lock files
package-lock.json
yarn.lock
pnpm-lock.yaml

# Other
.DS_Store
Thumbs.db
